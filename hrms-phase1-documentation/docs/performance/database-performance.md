# Database Performance

## Purpose
Define DB practices.

Tenant-first composite indexes; deterministic bounded pagination; list projections; batched joins/eager strategies to avoid N+1; DB aggregation; hierarchy query support (recursive CTE if appropriate); time-indexed audit/document/attendance queries; concurrency tests for leave ledger and room overlap; representative EXPLAIN/query-plan review before production.
